{
    "health": {
        "endpoints": {
            "liveness": "/healthz?service=liveness",
            "readiness": "/healthz?service=readiness"
        }
    },
    "platform_issuer": "{{request.baseUrl}}/auth"
}